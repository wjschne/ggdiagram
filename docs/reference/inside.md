# is an ob_point inside a shape ?

is an ob_point inside a shape ?

## Usage

``` r
inside(x, y)
```

## Arguments

- x:

  object

- y:

  object

## Value

numeric vector where 1 = inside, 0 = on, -1 = outside
